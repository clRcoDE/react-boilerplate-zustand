
import {AuthInterface} from '../../types/index'
import styles from './Auth.module.scss'

export default function Auth( props : AuthInterface) {
    return (
        <div>
            <h1>Auth</h1>
        </div>
    )
}
